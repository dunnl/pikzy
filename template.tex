\documentclass[convert]{standalone}
\usepackage{tikzit}
\input{tealeaves.tikzstyles}
\begin{document}
\input{<%= tikz_filename %>}
\end{document}

% To make a PNG using imagemagick
%\documentclass[convert]{standalone}

% To make a PDF only use
%\documentclass{standalone}

% This doesn't work in standalone
%\begin{equation}
%  \tikzfig{demo}
%\end{equation}

% This doesn't work in standalone
%\ctikzfig{demo}

% This works in standalone
%\input{<%= tikz_filename %>}
